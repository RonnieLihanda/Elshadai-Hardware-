const ExcelJS = require("exceljs"); const path = require("path"); async function checkExcel() { const wb = new ExcelJS.Workbook(); const filePath = path.join(__dirname, "../Elshadai's Hardware Musembe.xlsx"); try { await wb.xlsx.readFile(filePath); const sheet = wb.worksheets[0]; console.log("Sheet Name: " + sheet.name); sheet.getRow(1).values.forEach((v, i) => { console.log("Col " + i + ": " + v); }); sheet.getRows(2, 3).forEach((row, i) => { console.log("Row " + (i+2) + ": " + JSON.stringify(row.values)); }); } catch (err) { console.error("Error:", err.message); } } checkExcel();
