const ExcelJS = require("exceljs"); const path = require("path"); async function checkExcel() { const wb = new ExcelJS.Workbook(); const filePath = path.join(__dirname, "../inventory.xlsx"); try { await wb.xlsx.readFile(filePath); const sheet = wb.getWorksheet("SalesLog"); console.log("--- Sales Log Entries ---"); if (sheet) { sheet.eachRow((row, rowNumber) => { console.log("Row " + rowNumber + ": " + JSON.stringify(row.values)); }); } else { console.log("Sheet not found"); } } catch (err) { console.error("Error:", err.message); } } checkExcel();
